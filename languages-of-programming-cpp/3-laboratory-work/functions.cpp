//
// Created by bek on 27.10.2020.
//

#include "header.h"

/*template<typename T>
T generateRandomNumberInRange(int &beginning, int &end) {
    T number = roundTheNumber(beginning + (random() / ( RAND_MAX / (end - beginning))));
    return number;
}*/
